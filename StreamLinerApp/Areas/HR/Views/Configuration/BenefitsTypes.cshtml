@model IEnumerable<StreamLinerEntitiesLayer.HREntities.HRBenefitsType>

@{
    ViewData["Title"] = "Index";
    
}
<partial name="_ConfigurationMenu" />
<br />
<p>
    <a class="btn btn-primary" asp-action="CreateBenefitsType">   <i class="fa fa-plus"></i> Create New   </a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.HRBenefitsTypeName)
            </th>

            <th style="width:200px"></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.HRBenefitsTypeName)
                </td>

                <td>
                    <a asp-action="EditBenefitsType" class="btn btn-info" asp-route-id="@item.HRBenefitsTypeId"><i class="fa fa-eye"></i></a> |
                    <a asp-action="DeleteBenefitsType" class="btn btn-danger" asp-route-id="@item.HRBenefitsTypeId"><i class="fa fa-trash"></i></a>
                </td>
            </tr>
        }
    </tbody>
</table>
